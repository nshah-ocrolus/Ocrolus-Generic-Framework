# ============================================================
# MeridianLink Integration PoC — Environment Configuration
# Generic Framework
# ============================================================
# Copy this file to .env and fill in the values below.
# NEVER commit .env to version control.
# ============================================================

# --- Application Settings ---
PORT=4000
NODE_ENV=development

# --- Mock Mode ---
# Set to "true" to use simulated MeridianLink responses (no live API needed)
# Set to "false" to connect to the real MeridianLink API
USE_MOCK=true

# --- MeridianLink OAuth Credentials (required for live API) ---
# Get these from the Vendor Portal (see API Access Administration guide)
ML_CLIENT_ID=your_client_id
ML_CLIENT_SECRET=your_client_secret
ML_OAUTH_URL=https://playrunner.mortgage.meridianlink.com/oauth/token

# --- MeridianLink Generic Framework Domain ---
ML_BASE_DOMAIN=https://playrunner.mortgage.meridianlink.com

# --- MeridianLink Legacy Credentials (optional, for reference) ---
ML_BASE_URL=https://your-instance.meridianlink.com
ML_USERNAME=your_api_username
ML_PASSWORD=your_api_password

# --- Processing Settings ---
# Simulated processing delay in milliseconds (default: 2000)
PROCESSING_DELAY_MS=2000
